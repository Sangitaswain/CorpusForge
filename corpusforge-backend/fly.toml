app = "corpusforge-backend"
primary_region = "sin"

[build]

[mounts]
  source = "corpusforge_data"
  destination = "/app/data"

[http_service]
  internal_port = 8080
  force_https = true

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
